{
  "name": "bitbucket-server-receiver",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/bitbucket-server-receiver/src",
  "projectType": "application",
  "tags": ["firebase:function", "firebase:name:bitbucket-server-receiver", "firebase:dep:firebase-app"],
  "targets": {
    "build": {
      "executor": "@nx/esbuild:esbuild",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/apps/bitbucket-server-receiver",
        "main": "apps/bitbucket-server-receiver/src/main.ts",
        "tsConfig": "apps/bitbucket-server-receiver/tsconfig.app.json",
        "assets": [
          "apps/bitbucket-server-receiver/src/assets",
          {
            "glob": "**/*",
            "input": "apps/firebase-app/environment",
            "output": "."
          }
        ],
        "generatePackageJson": true,
        "platform": "node",
        "bundle": true,
        "thirdParty": false,
        "dependenciesFieldType": "dependencies",
        "target": "node24",
        "format": ["esm"],
        "esbuildOptions": {
          "logLevel": "info"
        }
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/bitbucket-server-receiver/jest.config.ts",
        "passWithNoTests": true
      },
      "configurations": {
        "ci": {
          "ci": true,
          "codeCoverage": true
        }
      }
    },
    "deploy": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nx run firebase-app:deploy --only functions:bitbucket-server-receiver"
      },
      "dependsOn": ["build"]
    }
  }
}
